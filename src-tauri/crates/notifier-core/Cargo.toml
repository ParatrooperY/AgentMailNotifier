[package]
name = "agent-mail-notifier-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml_edit = "0.23"

[dev-dependencies]
pretty_assertions = "1"
